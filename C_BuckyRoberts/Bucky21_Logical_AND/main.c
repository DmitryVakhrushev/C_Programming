#include <stdio.h>
#include <stdlib.h>

int main()
{
	int hoursStudied = 60; //Required 10 or more
	int kidsBeatUp = 0;    //Required 0 kids

	if( (hoursStudied >=10) && (kidsBeatUp==0) ){

		printf("You are a good student");
	}

    return 0;
}
